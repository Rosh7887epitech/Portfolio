/*
** EPITECH PROJECT, 2023
** my_size_list
** File description:
** return the size of the list
*/

#include "list.h"

int my_list_size (linked_list_t const * begin)
{
    inked_list_t *list,
    
    while (list != NULL)
    {
        my_strlen((char *)list ->data);
        list = list ->next;
    }
    return (0);
}

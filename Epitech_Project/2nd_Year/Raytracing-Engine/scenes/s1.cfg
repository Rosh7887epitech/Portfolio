// Configuration of the camera
camera = {
    resolution = { width = 1920.0, height = 1080.0 };
    position = { x = 0.0, y = 2.0, z = -55.0 };
    fieldOfView = 72.0;
};

// Primitives in the scene
primitives = {

    // List of cylinders
    cylinders = (
        { x = 2.5, y = 1.0, z = -60.0, radius = 1.0, height = 2.0, color = { r = 0.2, g = 0.8, b = 0.2, a = 1.0  } }
    );

    // List of cones
    cones = (
        { axis = "Y", x = -3.0, y = 1.0, z = -61.0, radius = 1.5, height = 3.0, color = { r = 0.0, g = 0.0, b = 1.0, a = 1.0  } }
    );

    // List of planes
    planes = (
        { axis = "Y", x = 0.0, y = 0.0, z = 0.0, color = { r = 1.0, g = 1.0, b = 1.0, a = 1.0  } }
    );
};

// Light configuration
lights = {
    ambient = 1.0,
    diffuse = 0.6,
    point = ();
    directional = (
        { direction = { x = 5.0, y = 4.0, z = 0.0 }, color = { r = 1.0, g = 1.0, b = 1.0 }, intensity = 2.0 }
    );
};
